# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

platform :ios, "10.0"

target 'ARGame' do
    use_frameworks!
  
  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
  pod 'GoogleMaps'
  #pod 'GooglePlaces'

end
